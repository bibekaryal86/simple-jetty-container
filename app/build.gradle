/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.4.1/userguide/tutorial_java_projects.html
 */

plugins {
    id 'java'
    id 'application'
    id 'com.diffplug.spotless' version '6.21.0'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

repositories {
    mavenCentral()
}

jar {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE

    manifest {
        attributes "Main-Class": "simple.jetty.container.App"
    }

    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

dependencies {
    implementation "org.eclipse.jetty:jetty-server:12.0.1"
    implementation "org.eclipse.jetty:jetty-security:12.0.1"
    implementation "org.eclipse.jetty:jetty-servlet:11.0.16"
    implementation "org.eclipse.jetty:jetty-webapp:11.0.16"
    implementation "org.eclipse.jetty:jetty-annotations:11.0.16"
    implementation "org.eclipse.jetty:jetty-jmx:12.0.1"
    implementation "org.eclipse.jetty:apache-jsp:11.0.16"
    implementation 'org.eclipse.jetty:apache-jstl:11.0.0'
}

application {
    // Define the main class for the application.
    mainClass = 'simple.jetty.container.App'
}

spotless {
    java {
        removeUnusedImports()
        googleJavaFormat()
    }
}
